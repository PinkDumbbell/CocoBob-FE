export interface LocationType {
  latitude: number;
  longitude: number;
}
export interface IGeolocationOptions {
  enableHighAccuracy?: boolean;
  maximumAge?: number;
  timeout?: number;
}
